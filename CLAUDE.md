## Project

Type: html
Deploy: github-pages

## Structure

src/styles/style.css, src/scripts/script.js, src/scripts/build-info.js, assets/images/

## Rules

- No build step, no npm
- Uses opentype.js from CDN for font parsing
- CSS vars from settings widget, never hardcode colors
- MUST have favicon.svg + favicon.png + favicon.ico in assets/images/
